export type ThemeColors = {
    primary: string;
    secondary: string;
    score: string;
    accent: string;
    background: string;
    text: string;
    secondary_text: string;
    [key: string]: string;
};